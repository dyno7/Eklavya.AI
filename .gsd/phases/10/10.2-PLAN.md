---
phase: 10
plan: 2
wave: 2
depends_on: ["10.1"]
files_modified:
  - eklavya_mobile/lib/core/services/dashboard_service.dart
  - eklavya_mobile/lib/features/dashboard/home_tab.dart
autonomous: true
user_setup: []
must_haves:
  truths:
    - "TaskClaimResult data class exists and completeTask() returns it"
    - "Floating XP toast animates upward on task completion"
    - "Level-up modal appears correctly on level boundary crossing"
    - "Badge unlock shows as a SnackBar if badgesAwarded non-empty"
  artifacts:
    - "home_tab.dart uses TaskClaimResult response"
---

# Plan 10.2: Flutter XP Gain Overlay & Level-Up Modal

<objective>
Make earning XP feel great. Replace the plain snackbar with a floating "+XP" animation that rises from the tapped task. Show a full level-up bottom sheet when the user crosses a level boundary. Parse the enhanced claim response from Plan 10.1.

Purpose: Increase engagement through game-like celebration UX.
Output: Updated DashboardService logic and new Home Tab visual overlays.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- eklavya_mobile/lib/features/dashboard/home_tab.dart
- eklavya_mobile/lib/core/services/dashboard_service.dart
</context>

<tasks>

<task type="auto">
  <name>Update DashboardService.completeTask to parse enhanced claim response</name>
  <files>
    eklavya_mobile/lib/core/services/dashboard_service.dart
  </files>
  <action>
    1. Define `TaskClaimResult` class.
    2. Change `completeTask` return type from `(int, int)?` to `TaskClaimResult?`.
    3. Parse new JSON fields: `bonus_xp`, `level_up`, `new_level`, `badges_awarded`.
    AVOID: Changing the URL or HTTP method because existing logic works. Keep null-safety strong.
  </action>
  <verify>flutter build web --no-tree-shake-icons</verify>
  <done>completeTask returns TaskClaimResult with all 6 fields populated from API response.</done>
</task>

<task type="auto">
  <name>Floating XP toast overlay + Level-Up modal in home_tab.dart</name>
  <files>
    eklavya_mobile/lib/features/dashboard/home_tab.dart
  </files>
  <action>
    1. Add `_showXpToast(BuildContext ctx, int xp, int bonusXp)` using `OverlayEntry`.
    2. Add `_showLevelUpModal(BuildContext ctx, int newLevel)` using `showModalBottomSheet`.
    3. Update `_onTaskComplete` to trigger the overlays based on `TaskClaimResult`.
    AVOID: Using `Navigator.of(context).push` for the level-up modal because bottom sheets look better. Do not remove `_fetchDashboard` call.
  </action>
  <verify>flutter test</verify>
  <done>Floating XP toast and Level-up modal displayed dynamically based on claim results.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] TaskClaimResult is instantiated properly when completing a task
- [ ] Modal displays only on level boundary transitions
- [ ] Animation completes smoothly without dropping frames
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
