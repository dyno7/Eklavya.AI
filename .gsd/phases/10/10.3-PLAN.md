---
phase: 10
plan: 3
wave: 2
depends_on: ["10.1"]
files_modified:
  - eklavya_mobile/lib/features/dashboard/home_tab.dart
  - eklavya_mobile/lib/features/profile/profile_tab.dart
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Task cards on Home tab show icon circle, time, XP pill, difficulty dot"
    - "Checkbox animation bounces on completion"
    - "Earned badge cards shimmer with golden border on Profile tab"
    - "No visual regression in other tabs"
  artifacts:
    - "Profile tab updated with flutter_animate shimmer"
---

# Plan 10.3: Rich Task Cards + Badge Shimmer on Profile

<objective>
Polish the task list on Home tab with richer cards. Add a shimmer glow to earned badges on Profile tab so they pop visually.

Purpose: Provide user with clearer visual hierarchy of task difficulty and improved reward tangibility.
Output: Refactored `_TaskCard` and `_BadgeCard` widgets.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- eklavya_mobile/lib/features/dashboard/home_tab.dart
- eklavya_mobile/lib/features/profile/profile_tab.dart
- eklavya_mobile/lib/core/theme/app_colors.dart
</context>

<tasks>

<task type="auto">
  <name>Replace plain ListTile task card with rich task card in home_tab.dart</name>
  <files>
    eklavya_mobile/lib/features/dashboard/home_tab.dart
  </files>
  <action>
    Replace the `Container > ListTile` block with a custom card widget `_TaskCard`.
    Add layout items: Left icon circle, title, subtitle row (time string, XP pill, difficulty dot)
    Checkbox interaction: wrap in `GestureDetector` + `AnimatedScale` that bounces.
    AVOID: Changing the `onChanged` logic because it's wired properly. Ensure it doesn't overflow horizontally.
  </action>
  <verify>flutter build web --no-tree-shake-icons</verify>
  <done>Each task card shows type icon circle, task title, estimated time, XP pill, difficulty dot.</done>
</task>

<task type="auto">
  <name>Add shimmer glow to earned badges in profile_tab.dart</name>
  <files>
    eklavya_mobile/lib/features/profile/profile_tab.dart
  </files>
  <action>
    In `_BadgeCard.build`: Use `flutter_animate` shimmer effect when `isEarned == true`. Add golden border.
    When `isEarned == false`: Keep Opacity 0.35 look.
    AVOID: Infinite rebuild loops with flutter_animate (use onPlay correctly). Do NOT change badge data fetching logic.
  </action>
  <verify>flutter build web --no-tree-shake-icons</verify>
  <done>Earned badges shimmer and have a golden border.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Task card UI matches design spec
- [ ] Checkboxes animate smoothly
- [ ] Only earned badges shimmer on Profile
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
