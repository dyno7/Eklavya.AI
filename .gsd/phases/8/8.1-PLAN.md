---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Database Migrations & Profile/Notification APIs

## Objective
Establish the backend structure for Phase 8 capabilities (badges, notifications) to support the Profile and Notifications UI before the frontend works on them.

## Context
- .gsd/SPEC.md
- eklavya_backend/app/presentation/users.py
- eklavya_backend/app/presentation/notifications.py (New)
- eklavya_backend/app/core/database.py

## Tasks

<task type="auto">
  <name>Database Migration SQL</name>
  <files>eklavya_backend/migration_phase8.sql</files>
  <action>
    Create a standalone SQL migration file.
    - Define a `badges` table (id, name, description, icon_url, required_xp).
    - Define a `user_badges` link table (user_id, badge_id, earned_at).
    - Define a `notifications` table (id, user_id, title, message, type, read_status, created_at).
    - Do NOT run this migration automatically; provide it so it can be pasted into Supabase SQL Editor.
  </action>
  <verify>cat eklavya_backend/migration_phase8.sql</verify>
  <done>SQL file exists with complete syntax for the 3 tables and RLS policies</done>
</task>

<task type="auto">
  <name>Backend Notification & Profile APIs</name>
  <files>eklavya_backend/app/presentation/notifications.py, eklavya_backend/app/presentation/users.py, eklavya_backend/app/main.py</files>
  <action>
    Implement API endpoints for the new schema.
    - Create a `notifications.py` router with `GET /` and `POST /{id}/read`.
    - Modify `users.py` (or create a new endpoint) to return a full list of all available badges combined with boolean flags representing whether the current user has earned them (`user_badges`).
    - Register the new routers in `main.py`.
  </action>
  <verify>uv run uvicorn app.main:app --reload</verify>
  <done>Endpoints are served without crashing</done>
</task>

## Success Criteria
- [ ] `migration_phase8.sql` is ready to execute in Supabase dashboard.
- [ ] Notifications and Badges APIs exist on the FastAPI backend.
