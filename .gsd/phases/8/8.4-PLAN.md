---
phase: 8
plan: 4
wave: 2
---

# Plan 8.4: Profile Live Data & Notifications Screen

## Objective
Replace hardcoded profile stats with live DB data, including a new locked/earned badge system, and create a notifications screen connected to real-time events.

## Context
- eklavya_mobile/lib/features/profile/profile_tab.dart
- eklavya_mobile/lib/core/services/user_service.dart (New)
- eklavya_mobile/lib/features/notifications/notifications_screen.dart (New)

## Tasks

<task type="auto">
  <name>Live Profile Stats & Badges</name>
  <files>eklavya_mobile/lib/features/profile/profile_tab.dart, eklavya_mobile/lib/core/services/user_service.dart</files>
  <action>
    Fetch real stats and badges from the backend.
    - Create `UserService` to fetch badges (`/api/v1/users/me/badges`).
    - Use `shimmer` or a container with an animated gradient for skeleton loaders while `DashboardService` / `UserService` requests are pending.
    - Display badges in a wrap layout: earned badges show brightly with icons, unearned badges show greyed-out or locked.
    - Add a back button pointing to the home tab or use `context.pop()` at the top left of `profile_tab.dart`.
  </action>
  <verify>grep "Shimmer" eklavya_mobile/lib/features/profile/profile_tab.dart</verify>
  <done>Stats and badges load lazily and display real DB states</done>
</task>

<task type="auto">
  <name>Notifications Screen & Bell Icon</name>
  <files>eklavya_mobile/lib/features/notifications/notifications_screen.dart, eklavya_mobile/lib/features/dashboard/home_tab.dart</files>
  <action>
    Build the notifications feature.
    - Create `NotificationsScreen` that fetches data from `/api/v1/notifications`. Provide a nice UI for the list of notifications.
    - Setup Supabase Realtime channel on the `notifications` table so the bell icon on the home tab instantly gets a red dot when a new record is inserted.
    - Make the home tab bell tap navigate to `/notifications` and marking them red clears the dot.
  </action>
  <verify>cat eklavya_mobile/lib/features/notifications/notifications_screen.dart</verify>
  <done>Notifications screen works and bell icon reflects real-time unread count</done>
</task>

## Success Criteria
- [ ] Tapping the bell icon navigates to the new Notifications screen.
- [ ] Profile tab renders skeleton loaders before displaying live XP/Streaks and dynamic locked/unlocked Badges.
